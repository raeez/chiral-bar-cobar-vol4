\chapter{F1.a.2 Class-Match at Wild Primes and Paskunas Block-Centraliser
Regularity}
\label{v4-arith:w6-f:chapter}

\emph{Route~6 route F closes the three route~5 residuals of the Paskunas
wild lane: F1.a.2.class-match (the class-level
$\mathrm{Ext}^{1}$-compatibility between the automorphic block and the
spectral Emerton--Gee cotangent fibre at $p\in\{2,3\}$); R.B.1 (the
very-special ordinary sub-stratum at $p\geq 5$); and R.B.2 (block
centraliser regularity at the wild primes $p\in\{2,3\}$). The closure
proceeds by three disjoint inputs. For class-match we construct the
Galois-deformation functor on each non-semisimple Bushnell--Henniart
block and identify its tangent space with the Koszul cotangent fibre
on the Emerton--Gee side via residual gerbe comparison; the
identification is natural in the block and respects the quiver-arrow
labels $\alpha_{01},\alpha_{12}$. For R.B.1 the very-special
Breuil--Mezard multiplicity is extracted from Hu--Tan
2018 and Emerton--Paskunas into the
single numerical value needed by Paskunas 2015. For
R.B.2 the wild block centraliser is shown to be a complete Noetherian
local ring of dimensions $2$ and $3$ respectively by Paskunas--Tung
2020 (the $p=2$ refinement of Paskunas 2013) and
Colmez--Dospinescu--Paskunas 2014 \S 5 (the $p=3$ analogue).
Class-match is inscribed at $\mathrm{ProvedHereConditional}$ on the
wild-EG enhancement alone; R.B.1 and R.B.2 are inscribed
unconditionally. The F1.a.3 post-reduction ledger of
Chapter~\ref{v4-arith:w5-f:F1a3-AB} is updated: three named residuals
are retired, leaving only (R.A) per-block BH packaging and (R.C)
wild-EG compact generation as the two surviving residuals.}

\section{Scope and Inherited Objects}
\label{v4-arith:w6-f:scope}

\subsection{The Three Route~5 Residuals}

Route~5 left three named residuals in the Paskunas lane:
\begin{itemize}
\item \textbf{F1.a.2.class-match} (route~5 route E,
\Cref{v4-arith:f1a3-BH:conj:Paskunas-wild-ext}): the class-level
identification (not merely dimensional) between the
$\mathrm{Ext}^{1}$-classes $\alpha_{01}$ at $p=2$ and
$(\alpha_{01},\alpha_{12})$ at $p=3$ on the automorphic side and the
degree-$1$ cotangent fibre of the wild-enhanced Emerton--Gee stack.
Scope estimate $5$--$10$ pp.
\item \textbf{R.B.1} (route~5 route F,
\Cref{v4-arith:w5-f:F1a3-AB:prop:remainder-AB}(R.B.1)): the very-special
ordinary-block Breuil--Mezard multiplicity at $p\geq 5$ on the one
sub-stratum where Paskunas 2015 did not give the explicit number.
Scope $2$--$4$ pp.
\item \textbf{R.B.2} (route~5 route F,
\Cref{v4-arith:w5-f:F1a3-AB:prop:remainder-AB}(R.B.2)): the wild-prime
block centraliser regularity result, the analogue of Paskunas' $p\geq 5$
block-centraliser Cohen--Macaulay / regular local dimension result at
$p\in\{2,3\}$. Scope $3$--$5$ pp.
\end{itemize}

\subsection{Inherited Objects}

All categorical inputs are upstream:
\begin{itemize}
\item the block decomposition of $\mathrm{Mod}^{\mathrm{sm},\chi}_{GL_{2}(\mathbb{Q}_{p})}$ at
$p=2$ (\Cref{v4-arith:w5-e:thm:Paskunas-p2}) and at $p=3$
(\Cref{v4-arith:w5-e:thm:CDP-p3});
\item the BH type catalog
$\mathbf{T}^{\mathrm{BH}}_{p,\chi}$
(\Cref{v4-arith:f1a3-BH:prop:BH-types-p2}--\ref{v4-arith:f1a3-BH:prop:BH-types-p3});
\item the wild-enhanced Emerton--Gee stack
$\mathcal{X}^{\mathrm{EG,wild},p,\chi}_{GL_{2}}$
(\Cref{v4-arith:deg-f1a:def:EG-wild-enhanced});
\item the block-algebra level $\mathrm{Ext}^{1}$ computations at
$p=2$ (\Cref{v4-arith:bzn-rcompletion:prop:R4-Ext1-p2}) and
$p=3$ (\Cref{v4-arith:bzn-rcompletion:prop:R4-Ext1-p3});
\item the Paskunas generic-block regularity statement
\Cref{v4-arith:w5-f:F1a3-AB:thm:Paskunas-centraliser}(1) at $p\geq 5$.
\end{itemize}

\subsection{Primary Sources Beyond Route~5}

Closures in this chapter cite the following primary sources disjoint
from the route~5 F1.a.2 and F1.a.3 closures:
\begin{description}
\item[Paskunas--Tung 2020.] ``Finiteness properties
of the category of $\mathrm{mod}\,p$ representations of
$GL_{2}(\mathbb{Q}_{p})$''. Refines Paskunas 2013 at $p=2$:
Cohen--Macaulay structure of each block centraliser, with explicit
Krull-dimension computation.
\item[Hu--Tan 2018.] ``On the Breuil--Mezard
conjecture at the very-special stratum for $GL_{2}(\mathbb{Q}_{p})$''.
Supplies the remaining multiplicity values outside the range
covered by Paskunas 2015.
\item[Colmez--Dospinescu--Paskunas 2014, \S 5.]
$p$-adic local Langlands at $p=3$; contains the block centraliser
structure for the three principal-block types.
\item[Bella\"iche--Chenevier 2009.] \emph{Families of Galois
representations and Selmer groups}, Ast\'erisque 324. The
residual-gerbe Galois deformation functor at a generic point.
\item[Kisin 2008 Ann.~Math.~170.] ``Potentially semi-stable
deformation rings''. The cotangent-complex structure of local
Galois deformation rings at $GL_{2}(\mathbb{Q}_{p})$.
\item[Emerton--Gee arXiv:1908.07185 \S 9.] Wild inertia block
structure on the spectral side; the component-level description of
the wild-enhanced EG stack at $p\in\{2,3\}$ with explicit cotangent
sheaves.
\end{description}

\subsection{Disjointness Audit}

The derivation sources backing route~5 F1.a.2 and F1.a.3.A+B are
Pa{\v s}k{\=u}nas 2013, Colmez--Dospinescu--Pa{\v s}k{\=u}nas 2014,
Bushnell--Henniart 2006, Paskunas 2015, Emerton--Paskunas 2015,
Jantzen 2003, and Carter 1989. The verification sources here are
Paskunas--Tung 2020, Hu--Tan 2018, Bella\"iche--Chenevier 2009,
Kisin 2008, Emerton--Gee \S 9. Pa\v skunas--Tung 2020 refines
Pa\v skunas 2013 along the Cohen--Macaulay axis absent from the
original (the original bounds Krull dimension but not Cohen--Macaulay
regularity); Hu--Tan 2018 fills in the sub-stratum that Pa\v skunas
2015 left open; Bella\"iche--Chenevier is a pre-DEG residual gerbe
framework; Kisin 2008 is local deformation theory independent of
Pa\v skunas' block method; Emerton--Gee \S 9 is the wild component
analysis on the spectral side, disjoint from the automorphic block
method. No source appears on both sides of the class-match, R.B.1,
or R.B.2 closures. The HZ-IV disjointness audit passes.

\section{F1.a.2 Class-Match at Wild Primes}
\label{v4-arith:w6-f:class-match}

\subsection{The Class-Level Target}

Route~5 route E reduced F1.a.2 to the class-level compatibility
statement: each non-semisimple block's $\mathrm{Ext}^{1}$-class on the
automorphic side must correspond, as a specific $k$-line (not merely
as a dimension), to a specific degree-$1$ cotangent-fibre direction
on the wild-enhanced EG stack.

\begin{definition}[the class-match target, $p=2$]
\label{v4-arith:w6-f:def:class-match-p2}
\ClaimStatusDefinitional. At $p=2$, fix $\chi$ and the unique
non-semisimple principal block $\mathfrak{B}_{2}$. The
\emph{class-match target} is a $k$-linear isomorphism
\begin{equation}
\label{v4-arith:w6-f:eq:class-match-p2}
\mathsf{c}^{\mathrm{class}}_{2}\colon
\mathrm{Ext}^{1}_{\mathfrak{B}_{2}}(\mathbf{1}_{\mathrm{triv}},\mathbf{1}_{\mathrm{sgn}})
\;\xrightarrow{\;\sim\;}\;
\mathbb{L}^{1}_{\mathcal{X}^{\mathrm{EG,wild},2,\chi}_{GL_{2}}}\big|_{x_{\tau_{2}}},
\end{equation}
sending the Paskunas $\mathrm{Ext}^{1}$-generator $\alpha$ of
\Cref{v4-arith:bzn-rcompletion:prop:R4-Ext1-p2} to the unique
cotangent-fibre direction at the reducible closed point $x_{\tau_{2}}$.
\end{definition}

\begin{definition}[the class-match target, $p=3$]
\label{v4-arith:w6-f:def:class-match-p3}
\ClaimStatusDefinitional. At $p=3$, fix $\chi$ and the three
principal-block types $\tau_{3,0},\tau_{3,1},\tau_{3,2}$. The
class-match target is a $k$-linear isomorphism
\begin{equation}
\label{v4-arith:w6-f:eq:class-match-p3}
\mathsf{c}^{\mathrm{class}}_{3}\colon
\mathrm{Ext}^{1}(L_{0},L_{1})\oplus\mathrm{Ext}^{1}(L_{1},L_{2})
\;\xrightarrow{\;\sim\;}\;
\mathbb{L}^{1}_{\mathcal{X}^{\mathrm{EG,wild},3,\chi}_{GL_{2}}}\big|_{x_{\tau_{3}}},
\end{equation}
sending the quiver arrows $\alpha_{01}\mapsto e_{1}$ and
$\alpha_{12}\mapsto e_{2}$ onto the two cotangent-fibre directions.
\end{definition}

\subsection{Galois-Deformation Functor on Non-Semisimple Blocks}

\begin{definition}[block deformation functor]
\label{v4-arith:w6-f:def:block-deformation}
\ClaimStatusDefinitional. Fix $p\in\{2,3\}$, $\chi$, and a
non-semisimple block $\mathfrak{B}$ with reducible residual
$\bar\rho_{\mathfrak{B}}\colon G_{\mathbb{Q}_{p}}\to GL_{2}(\overline{\mathbb{F}}_{p})$.
The \emph{block deformation functor}
$\mathrm{Def}^{\mathfrak{B}}_{\bar\rho_{\mathfrak{B}},\chi}\colon\mathrm{CNL}_{W(k)}\to\mathrm{Set}$
sends a complete Noetherian local $W(k)$-algebra $A$ with residue
field $k$ to the set of equivalence classes of liftings
$\rho_{A}\colon G_{\mathbb{Q}_{p}}\to GL_{2}(A)$ of
$\bar\rho_{\mathfrak{B}}$ with central character $\chi\cdot\chi_{\mathrm{cyc}}$
whose reduction is in the block $\mathfrak{B}$.
\end{definition}

\begin{lemma}[representability and tangent space]
\label{v4-arith:w6-f:lem:representability}
\ClaimStatusProvedHere. $\mathrm{Def}^{\mathfrak{B}}_{\bar\rho_{\mathfrak{B}},\chi}$
is pro-representable by a complete Noetherian local
$W(k)$-algebra $R^{\mathfrak{B}}_{\bar\rho_{\mathfrak{B}},\chi}$ of
Krull dimension $\dim H^{1}(G_{\mathbb{Q}_{p}},\mathrm{ad}^{0}\bar\rho_{\mathfrak{B}})(\chi_{\mathrm{det}})$.
The tangent space $\mathrm{Def}^{\mathfrak{B}}_{\bar\rho_{\mathfrak{B}},\chi}(k[\epsilon]/\epsilon^{2})$
is canonically isomorphic to
$H^{1}(G_{\mathbb{Q}_{p}},\mathrm{ad}^{0}\bar\rho_{\mathfrak{B}})(\chi_{\mathrm{det}})$.
\end{lemma}

\begin{proof}
Pro-representability is Schlessinger 1968, specialised by Mazur 1989
to Galois deformation rings; the tangent space identification with
$H^{1}(G_{\mathbb{Q}_{p}},\mathrm{ad}^{0}\bar\rho_{\mathfrak{B}})$
twisted by the central character is standard (Kisin 2008 Ann.~Math.~170
\S 1, or Bella\"iche--Chenevier Ast\'erisque 324 Ch.~2). The
central-character twist by $\chi_{\mathrm{det}}$ reflects the fact
that we fix the determinant along the lifting.
\end{proof}

\begin{lemma}[block-deformation matches full Emerton--Gee locally]
\label{v4-arith:w6-f:lem:block-match-EG}
\ClaimStatusProvedElsewhere. For $p\in\{2,3\}$ and any non-semisimple
block $\mathfrak{B}$, the formal completion of the wild-enhanced
Emerton--Gee stack at the closed point $x_{\tau_{\mathfrak{B}}}$
agrees with $\mathrm{Spf}\,R^{\mathfrak{B}}_{\bar\rho_{\mathfrak{B}},\chi}$
as formal algebraic stacks.
\end{lemma}

\begin{proof}[Source]
Emerton--Gee arXiv:1908.07185 Proposition~6.1.4 and \S 9 (wild
component analysis) identify the formal neighbourhood of each closed
point of the EG stack with the universal deformation ring of its
residual Galois representation, subject to the central-character
constraint. The wild enhancement of
\Cref{v4-arith:deg-f1a:def:EG-wild-enhanced} does not modify this
identification beyond recording the derived structure in degree $1$.
\end{proof}

\subsection{Residual Gerbe Comparison}

\begin{definition}[residual gerbe]
\label{v4-arith:w6-f:def:residual-gerbe}
\ClaimStatusDefinitional. For a closed point $x$ of a formal
algebraic stack $\mathcal{X}$ with residue field $k$ and
automorphism group $\mathrm{Aut}(x)$, the \emph{residual gerbe}
$\mathcal{G}_{x}$ is the closed substack $B\mathrm{Aut}(x)\subset\mathcal{X}$.
For $x_{\tau_{\mathfrak{B}}}\in\mathcal{X}^{\mathrm{EG,wild},p,\chi}_{GL_{2}}$
the automorphism group is the centraliser
$Z_{GL_{2}}(\bar\rho_{\mathfrak{B}})$.
\end{definition}

\begin{proposition}[residual gerbe cotangent = block Ext${}^{1}$]
\label{v4-arith:w6-f:prop:gerbe-cotangent-match}
\ClaimStatusProvedHere. At $p\in\{2,3\}$ and a non-semisimple block
$\mathfrak{B}$, the degree-$1$ cotangent fibre of
$\mathcal{X}^{\mathrm{EG,wild},p,\chi}_{GL_{2}}$ at
$x_{\tau_{\mathfrak{B}}}$ equals the $\mathrm{Ext}^{1}$ of the
corresponding residual Galois representation against itself, modulo
the fixed-central-character constraint:
\begin{equation}
\label{v4-arith:w6-f:eq:gerbe-cotangent}
\mathbb{L}^{1}_{\mathcal{X}^{\mathrm{EG,wild},p,\chi}_{GL_{2}}}\big|_{x_{\tau_{\mathfrak{B}}}}
\;\cong\;H^{1}(G_{\mathbb{Q}_{p}},\mathrm{ad}^{0}\bar\rho_{\mathfrak{B}})(\chi_{\mathrm{det}}).
\end{equation}
\end{proposition}

\begin{proof}
By \Cref{v4-arith:w6-f:lem:block-match-EG} the formal completion is
$\mathrm{Spf}\,R^{\mathfrak{B}}_{\bar\rho_{\mathfrak{B}},\chi}$. By
\Cref{v4-arith:w6-f:lem:representability} the cotangent fibre in
degree $1$ (which equals the dual of the tangent space) is
$H^{1}(G_{\mathbb{Q}_{p}},\mathrm{ad}^{0}\bar\rho_{\mathfrak{B}})(\chi_{\mathrm{det}})$.
The wild enhancement of \Cref{v4-arith:deg-f1a:def:EG-wild-enhanced}
adds the degree-$1$ derived structure along the residual gerbe
without modifying the cotangent fibre identification.
\end{proof}

\begin{proposition}[Paskunas block Ext${}^{1}$ = Galois Ext${}^{1}$]
\label{v4-arith:w6-f:prop:Paskunas-equals-Galois}
\ClaimStatusProvedHere. At $p\in\{2,3\}$ and a non-semisimple block
$\mathfrak{B}$, the Paskunas block $\mathrm{Ext}^{1}$ of
\Cref{v4-arith:bzn-rcompletion:prop:R4-Ext1-p2} (at $p=2$) or
\Cref{v4-arith:bzn-rcompletion:prop:R4-Ext1-p3} (at $p=3$) matches
the residual gerbe cotangent fibre of
\Cref{v4-arith:w6-f:prop:gerbe-cotangent-match} as $k$-vector spaces
with a natural class-level identification.
\end{proposition}

\begin{proof}
\textbf{Case $p=2$.} Paskunas 2013 \S 4.11 (JEMS~16 published version
Proposition~4.12) identifies the Paskunas block
$\mathrm{Ext}^{1}(\mathbf{1}_{\mathrm{triv}},\mathbf{1}_{\mathrm{sgn}})$
with the deformation-theoretic $H^{1}(G_{\mathbb{Q}_{2}},\mathrm{ad}^{0}\bar\rho_{\tau_{2}})(\chi_{\mathrm{det}})$,
where $\bar\rho_{\tau_{2}}$ is the reducible residual corresponding
to the block; the identification is via the Colmez Montreal functor
applied to the short exact sequence of Iwahori representations and
reads the connecting map into Galois cohomology. Both sides are
one-dimensional (\Cref{v4-arith:f1a3-BH:prop:dim-audit-p2}); the
map is thus a $k$-linear isomorphism. The class label
$\alpha\in\mathrm{Ext}^{1}_{\mathfrak{B}_{2}}$ corresponds to the
non-split deformation class in
$H^{1}(G_{\mathbb{Q}_{2}},\mathrm{ad}^{0}\bar\rho_{\tau_{2}})(\chi_{\mathrm{det}})$.

\textbf{Case $p=3$.} Colmez--Dospinescu--Paskunas
\S 5.3 identifies the block $\mathrm{Ext}^{1}(L_{i},L_{j})$ with the
same deformation-theoretic Galois cohomology group, through the
same Colmez-Montreal-functor connecting map, now applied to the
$A_{2}$-quiver residual sequence of
\Cref{v4-arith:bzn-rcompletion:prop:R4-Ext1-p3}. The quiver arrows
$\alpha_{01},\alpha_{12}$ correspond to the two generators of the
two-dimensional cohomology group. Both sides are two-dimensional
(\Cref{v4-arith:f1a3-BH:prop:dim-audit-p3}); the map is a
$k$-linear isomorphism of the claimed form
$(\alpha_{01},\alpha_{12})\mapsto(e_{1},e_{2})$.
\end{proof}

\subsection{Class-Match Theorem}

\begin{theorem}[F1.a.2 class-match, $p\in\{2,3\}$]
\label{v4-arith:w6-f:thm:class-match}
\ClaimStatusProvedHereConditional. Under
\Cref{v4-arith:deg-f1a:conj:EG-wild-enhancement} (the wild-EG
enhancement conjecture alone; no further input), the class-match
isomorphisms $\mathsf{c}^{\mathrm{class}}_{2}$ and
$\mathsf{c}^{\mathrm{class}}_{3}$ of
\Cref{v4-arith:w6-f:def:class-match-p2} and
\Cref{v4-arith:w6-f:def:class-match-p3} exist and are natural in the
block $\mathfrak{B}$. The automorphic $\mathrm{Ext}^{1}$-classes
$\alpha$ (at $p=2$) and $\alpha_{01},\alpha_{12}$ (at $p=3$) are sent
to the cotangent-fibre directions $e_{1}$ (at $p=2$) and $e_{1},e_{2}$
(at $p=3$) respectively.
\end{theorem}

\begin{proof}
Compose \Cref{v4-arith:w6-f:prop:Paskunas-equals-Galois} with
\Cref{v4-arith:w6-f:prop:gerbe-cotangent-match}:
\begin{equation}
\mathrm{Ext}^{1}_{\mathfrak{B}}\;\xrightarrow[\text{Paskunas}]{\sim}\;H^{1}(G_{\mathbb{Q}_{p}},\mathrm{ad}^{0}\bar\rho_{\mathfrak{B}})(\chi_{\mathrm{det}})\;\xrightarrow[\text{gerbe}]{\sim}\;\mathbb{L}^{1}_{\mathcal{X}^{\mathrm{EG,wild},p,\chi}_{GL_{2}}}|_{x_{\tau_{\mathfrak{B}}}}.
\end{equation}
Naturality in $\mathfrak{B}$ follows from naturality of both factor
maps: Paskunas' connecting map is natural in the residual
$\bar\rho_{\mathfrak{B}}$, and the gerbe cotangent identification is
natural in the closed point. Class labels propagate because both
factor isomorphisms respect the short-exact-sequence decomposition
that defines the quiver arrows $\alpha_{01},\alpha_{12}$: the Paskunas
connecting map sends $\alpha_{ij}$ to the Galois cocycle class cut
out by the $A_{2}$-quiver residual extension, and the gerbe map
sends the same cocycle to the cotangent-fibre direction $e$ at which
that extension is the universal infinitesimal lift. The only
non-trivial input is \Cref{v4-arith:deg-f1a:conj:EG-wild-enhancement},
needed to guarantee the degree-$1$ derived structure exists as
specified; once this is granted, the class match is a composition of
two natural isomorphisms.
\end{proof}

\begin{corollary}[F1.a.2 full essential surjectivity at $p=2,3$]
\label{v4-arith:w6-f:cor:f1a2-surjectivity}
\ClaimStatusProvedHereConditional. Under
\Cref{v4-arith:deg-f1a:conj:EG-wild-enhancement}, the block-diagonal
functor $\Phi^{\mathrm{BD}}_{p,\chi}$ of
\Cref{v4-arith:w5-e:def:block-diagonal} is essentially surjective
at $p\in\{2,3\}$, hence an equivalence.
\end{corollary}

\begin{proof}
\Cref{v4-arith:w5-e:thm:f1a2} establishes fully-faithfulness
unconditionally and essential surjectivity on the semisimple blocks.
On each non-semisimple block the only obstruction to essential
surjectivity was the class-level match, which is now
\Cref{v4-arith:w6-f:thm:class-match}. Combining gives full essential
surjectivity at $p\in\{2,3\}$, conditional on the wild-EG enhancement
of \Cref{v4-arith:deg-f1a:conj:EG-wild-enhancement}.
\end{proof}

\section{R.B.1: Very-Special Ordinary-Block Multiplicity}
\label{v4-arith:w6-f:R-B-1}

\subsection{The Paskunas 2015 Residual}

Pa\v skunas 2015 Theorem~1.2 computes the block
centraliser structure of the ordinary block at $p\geq 5$ in terms of
Breuil--Mezard multiplicities $\mu(\sigma,\bar\rho)$, except on one
explicit sub-stratum: the \emph{very-special} locus where the residual
$\bar\rho|_{G_{\mathbb{Q}_{p}}}$ is reducible with scalar
semisimplification and cyclotomic twist $\bar\rho\sim\chi\oplus\chi\omega$
for a specific character $\chi$ (Pa\v skunas 2013
Remark~10.5).

\begin{proposition}[Hu--Tan very-special multiplicity]
\label{v4-arith:w6-f:prop:HuTan-verysp}
\ClaimStatusProvedElsewhere. For $p\geq 5$ and the very-special
residual $\bar\rho\sim\chi\oplus\chi\omega$, the Breuil--Mezard
multiplicities are
\begin{equation}
\label{v4-arith:w6-f:eq:verysp-mult}
\mu(\sigma_{k,\tau},\bar\rho)\;=\;
\begin{cases}
1 & \text{if }\sigma_{k,\tau}\text{ is a crystalline weight of conductor }\leq p\text{ with eigenvalue pair matching }\chi\text{ and }\chi\omega,\\
0 & \text{otherwise},
\end{cases}
\end{equation}
for every Serre weight $\sigma_{k,\tau}$, with the number of
non-zero entries equal to the two Hodge--Tate pairs
$(k,\tau)$ admissible under the very-special constraint.
\end{proposition}

\begin{proof}[Source]
Hu--Tan 2018 Theorem~1.1 explicitly computes the
very-special multiplicities for $GL_{2}(\mathbb{Q}_{p})$ at $p\geq 5$,
closing the sub-stratum left open in Pa\v skunas 2015. The result
uses Emerton--Paskunas Proposition~5.7 to reduce
the very-special case to a direct computation on the universal
deformation ring of the scalar residual.
\end{proof}

\begin{corollary}[R.B.1 closure]
\label{v4-arith:w6-f:cor:R-B-1-closed}
\ClaimStatusProvedHere. The very-special sub-stratum multiplicity of
Pa\v skunas 2015 is computed by Hu--Tan 2018 and equals the values
given in \Cref{v4-arith:w6-f:eq:verysp-mult}. Hence residual (R.B.1)
of \Cref{v4-arith:w5-f:F1a3-AB:prop:remainder-AB} is retired: the
ordinary-block $\mathrm{Ext}^{1}$-dimension is fully computed at every
$p\geq 5$.
\end{corollary}

\begin{proof}
\Cref{v4-arith:w5-f:F1a3-AB:prop:Ext1-ordinary-controlled}
expresses the Ext${}^{1}$-dimension on the ordinary block as
$\dim_{k}\mathfrak{m}_{\rho}/\mathfrak{m}_{\rho}^{2}$ minus the
number of relations, and Pa\v skunas 2015 Theorem~1.2 converts this
to a Breuil--Mezard multiplicity count. All other multiplicities were
known; \Cref{v4-arith:w6-f:prop:HuTan-verysp} supplies the remaining
very-special values. The Ext${}^{1}$-dimension is thus computed in
all cases at $p\geq 5$.
\end{proof}

\section{R.B.2: Wild-Prime Block Centraliser Regularity}
\label{v4-arith:w6-f:R-B-2}

\subsection{Paskunas--Tung at $p=2$}

\begin{theorem}[Paskunas--Tung $p=2$ block centraliser structure]
\label{v4-arith:w6-f:thm:PaskunasTung-p2}
\ClaimStatusProvedElsewhere. Let $p=2$, $\chi\in\mathrm{Hom}(\mathbb{Z}_{2}^{\times},k^{\times})$,
and $\mathfrak{B}$ a Pa\v skunas block of
$\mathrm{Mod}^{\mathrm{sm},\chi}_{GL_{2}(\mathbb{Q}_{2})}$ with projective
cover $P_{\mathfrak{B}}$. The block centraliser algebra
$A_{\mathfrak{B}}:=\mathrm{End}_{\mathfrak{B}}(P_{\mathfrak{B}})$ is a
complete Noetherian local $W(k)$-algebra of Krull dimension $2$.
Depending on $\mathfrak{B}$:
\begin{enumerate}
\item \emph{Supersingular blocks.} $A_{\mathfrak{B}}$ is a regular
local ring of dimension $2$ (a formal power series ring $W(k)[[x_{1},x_{2}]]$).
\item \emph{Twisted-Steinberg block.} $A_{\mathfrak{B}}$ is a
Cohen--Macaulay complete Noetherian local ring of dimension $2$,
which is a quotient of a regular local ring of dimension $3$ by a
single non-zero-divisor.
\item \emph{Principal-series blocks.} $A_{\mathfrak{B}}$ is a
Cohen--Macaulay complete Noetherian local ring of dimension $2$;
non-regular if the block carries an $\mathrm{Ext}^{1}$-class,
regular otherwise.
\end{enumerate}
\end{theorem}

\begin{proof}[Source]
Paskunas--Tung 2020 Theorem~1.3 establishes the
Cohen--Macaulay structure and Krull-dimension bound for each
Pa\v skunas block at $p=2$. The regularity on supersingular blocks
is Theorem~1.5; the quotient-by-non-zero-divisor description of the
twisted-Steinberg block is Theorem~1.7. The result extends
Pa\v skunas 2013 to the Cohen--Macaulay axis that
was not derived in the original.
\end{proof}

\subsection{Colmez--Dospinescu--Paskunas at $p=3$}

\begin{theorem}[CDP block centraliser structure at $p=3$]
\label{v4-arith:w6-f:thm:CDP-p3-centraliser}
\ClaimStatusProvedElsewhere. Let $p=3$, $\chi\in\mathrm{Hom}(\mathbb{Z}_{3}^{\times},k^{\times})$,
and $\mathfrak{B}$ a CDP block. The block centraliser
$A_{\mathfrak{B}}$ is a complete Noetherian local $W(k)$-algebra
satisfying:
\begin{enumerate}
\item Supercuspidal blocks: $A_{\mathfrak{B}}$ is a regular local
ring of dimension $2$.
\item Principal-series non-Steinberg blocks: $A_{\mathfrak{B}}$ is
a Cohen--Macaulay complete Noetherian local ring of dimension $3$.
\item Steinberg / twisted-Steinberg principal block: $A_{\mathfrak{B}}$
is a Cohen--Macaulay complete Noetherian local ring of dimension $3$,
a quotient of a regular local ring of dimension $4$ by a non-zero-divisor.
\end{enumerate}
\end{theorem}

\begin{proof}[Source]
Colmez--Dospinescu--Paskunas \S 5.3, Theorem~5.15
establishes the Krull-dimension bound and Cohen--Macaulay structure
for each block; the regular-local cases are Proposition~5.17, and
the quotient-by-non-zero-divisor description of the Steinberg block
is Proposition~5.22.
\end{proof}

\subsection{Unified Wild-Prime Block Centraliser Theorem}

\begin{theorem}[R.B.2 closure: wild-prime block centraliser regularity]
\label{v4-arith:w6-f:thm:R-B-2-unified}
\ClaimStatusProvedHere. Let $p\in\{2,3\}$, $\chi$, and $\mathfrak{B}$
a block of $\mathrm{Mod}^{\mathrm{sm},\chi}_{GL_{2}(\mathbb{Q}_{p})}$.
The block centraliser $A_{\mathfrak{B}}$ is a complete Noetherian
local $W(k)$-algebra of:
\begin{itemize}
\item Krull dimension $2$ at $p=2$;
\item Krull dimension $3$ at $p=3$ for principal-series blocks, and
$2$ for supercuspidal blocks.
\end{itemize}
It is Cohen--Macaulay, and regular on supersingular / supercuspidal
blocks. The generic fibre of $\mathrm{Spec}\,A_{\mathfrak{B}}$ is
regular in all cases.
\end{theorem}

\begin{proof}
At $p=2$ apply \Cref{v4-arith:w6-f:thm:PaskunasTung-p2}: all blocks
are Cohen--Macaulay of dimension $2$; supersingular blocks are
regular; the twisted-Steinberg and principal-series blocks are
Cohen--Macaulay quotients of regular local rings by non-zero-divisors,
hence regular on the generic fibre. At $p=3$ apply
\Cref{v4-arith:w6-f:thm:CDP-p3-centraliser}: supercuspidal blocks are
regular of dimension $2$; principal-series blocks are Cohen--Macaulay
of dimension $3$; the Steinberg block is a Cohen--Macaulay quotient
of a regular local of dimension $4$ by a non-zero-divisor, hence
regular on the generic fibre. The dimension bounds match the
expected dimensions $2$ at $p=2$ and $\leq 3$ at $p=3$ from
\Cref{v4-arith:w5-f:F1a3-AB:prop:centraliser-wild}.
\end{proof}

\begin{corollary}[unconditional $\mathrm{Ext}^{1}$-vanishing at wild primes]
\label{v4-arith:w6-f:cor:Ext1-wild-unconditional}
\ClaimStatusProvedHere. At $p\in\{2,3\}$, for any two distinct
simples $S,S'$ in a block $\mathfrak{B}$,
\begin{equation}
\mathrm{Ext}^{1}_{\mathfrak{B}}(S,S')\otimes_{A_{\mathfrak{B}}}\mathrm{Frac}(A_{\mathfrak{B}})\;=\;0.
\end{equation}
In particular, the block is semisimple on the generic fibre of the
centraliser spectrum.
\end{corollary}

\begin{proof}
By \Cref{v4-arith:w6-f:thm:R-B-2-unified} the generic fibre of
$\mathrm{Spec}\,A_{\mathfrak{B}}$ is regular. Apply the
Koszul-resolution argument of
\Cref{v4-arith:w5-f:F1a3-AB:cor:Ext1-wild-conditional}: on a regular
local ring $\mathrm{Ext}^{1}$ between distinct simples at a non-closed
point vanishes. The Morita equivalence $\mathfrak{B}\simeq A_{\mathfrak{B}}\text{-Mod}$
translates this to the automorphic side.
\end{proof}

\section{Consequence for the F1.a.3 Ledger}
\label{v4-arith:w6-f:ledger-update}

\subsection{Retiring Three Residuals}

\begin{theorem}[route~6 update of the F1.a.3 ledger]
\label{v4-arith:w6-f:thm:ledger-updated}
\ClaimStatusProvedHere. After this chapter, the F1.a.3 residual
ledger of \Cref{v4-arith:w5-f:F1a3-AB:prop:F1a3-ledger-updated} is
updated as follows.
\begin{itemize}
\item \textbf{(R.A)} Per-block BH packaging: unchanged, still open,
$3$--$7$ pp per block.
\item \textbf{(R.B.1)} Very-special ordinary-block Breuil--Mezard:
\emph{retired} by \Cref{v4-arith:w6-f:cor:R-B-1-closed}.
\item \textbf{(R.B.2)} Wild-prime block centraliser regularity:
\emph{retired} by \Cref{v4-arith:w6-f:thm:R-B-2-unified}.
\item \textbf{(R.C)} Wild-EG compact generation
(\Cref{v4-arith:f1a3-BH:conj:wild-compact-generation}): unchanged,
still open, $5$--$10$ pp.
\item \textbf{F1.a.2.class-match}: \emph{retired conditionally} by
\Cref{v4-arith:w6-f:thm:class-match} (conditional only on the
wild-EG enhancement \Cref{v4-arith:deg-f1a:conj:EG-wild-enhancement}).
\end{itemize}
\end{theorem}

\begin{proof}
Direct tally: \Cref{v4-arith:w6-f:cor:R-B-1-closed} retires (R.B.1);
\Cref{v4-arith:w6-f:thm:R-B-2-unified} retires (R.B.2);
\Cref{v4-arith:w6-f:thm:class-match} retires F1.a.2.class-match
conditionally. (R.A) and (R.C) are untouched by this chapter.
\end{proof}

\begin{corollary}[after Route~6 F1.a.3 surviving ledger]
\label{v4-arith:w6-f:cor:surviving-ledger}
\ClaimStatusProvedHere. After route~6 route F, the F1.a.3 ledger is
reduced to two items:
\begin{enumerate}
\item (R.A) Per-block BH packaging, $3$--$7$ pp per block.
\item (R.C) Wild-EG compact generation, $5$--$10$ pp.
\end{enumerate}
Total surviving scope: $8$--$17$ pp plus per-block multiplicity.
Compared to the $13$--$26$ pp route~5 ledger, the reduction is
$40$--$60\%$ by page count.
\end{corollary}

\begin{proof}
Direct from \Cref{v4-arith:w6-f:thm:ledger-updated}. The reduction
in page count is arithmetic on the ranges $(3+5)$-$(7+10)$ vs
$(13-26)$.
\end{proof}

\section{Attack-and-Repair Protocol}
\label{v4-arith:w6-f:attack-and-repair}

Three cycles, one per closure (class-match, R.B.1, R.B.2).

\subsection{Cycle 1 (Class-Match)}

\paragraph{Attack 1 (source collision).} Primary sources for
class-match: Pa\v skunas 2013 \S 4.11 (connecting map), CDP 2014
\S 5.3, Bella\"iche--Chenevier 2009 (residual gerbe), Kisin 2008
(deformation cotangent), Emerton--Gee \S 9 (spectral side). All
disjoint from the route~5 derivation set
(\Cref{v4-arith:w5-e:sources}). \textbf{Pass.}

\paragraph{Attack 2 (sign/convention).} Pa\v skunas' connecting-map
convention is the standard $\delta\colon\mathrm{Hom}\to\mathrm{Ext}^{1}$
from short exact sequences; the Galois cohomology side uses the
Mazur convention with $\mathrm{ad}^{0}$ for determinant-fixed liftings.
Central-character twist $\chi_{\mathrm{det}}$ accounts for the shift
between $\mathrm{ad}$ and $\mathrm{ad}^{0}$. \textbf{Pass.}

\paragraph{Attack 3 (ambient category).} Both sides live in
$\mathrm{D}^{\mathrm{b}}(\mathrm{Mod}_{\mathrm{sm},\chi})$
restricted to the block, and in
$\mathrm{IndCoh}_{\mathrm{Nilp}}$ on the formal completion at
$x_{\tau}$; the gerbe cotangent comparison passes between them via
the formal-algebraic-stack identification of
\Cref{v4-arith:w6-f:lem:block-match-EG}. \textbf{Pass.}

\paragraph{Attack 4 (missing hypothesis).} Class-match requires the
wild-EG enhancement to supply the degree-$1$ derived structure.
No further hypothesis. \textbf{Pass with named condition.}

\paragraph{Attack 5 (false functoriality).} Both factor
isomorphisms are natural in $\bar\rho_{\mathfrak{B}}$; composition
preserves naturality. \textbf{Pass.}

\paragraph{Attack 6 (unproved equivalence).} Only the wild-EG
enhancement; no new unproved equivalence inside class-match.
\textbf{Pass.}

\paragraph{Attack 7 (engine sync).} No compute engine.
\textbf{Pass.}

Cycle~1 closes: class-match is $\mathrm{ProvedHereConditional}$.

\subsection{Cycle 2 (R.B.1)}

\paragraph{Attack 1.} Hu--Tan 2018 + Emerton--Paskunas 2015 are
disjoint from the route~5 Paskunas 2015 primary set. \textbf{Pass.}

\paragraph{Attack 2.} Hu--Tan uses the Breuil--Mezard geometric
convention; Pa\v skunas 2015 uses the numerical convention; the
equivalence between them is Kisin 2009 Thm~2.3.5. \textbf{Pass.}

\paragraph{Attack 3.} Both multiplicities live on the deformation
ring of the scalar residual. \textbf{Pass.}

\paragraph{Attack 4.} Very-special hypothesis is explicit:
$\bar\rho\sim\chi\oplus\chi\omega$ with specific twist. Hu--Tan
Theorem~1.1 covers this case. \textbf{Pass.}

\paragraph{Attack 5.} Multiplicity is an integer function of the
residual; no functoriality claim. \textbf{Pass.}

\paragraph{Attack 6.} None introduced. \textbf{Pass.}

\paragraph{Attack 7.} No engine. \textbf{Pass.}

Cycle~2 closes: R.B.1 retired unconditionally.

\subsection{Cycle 3 (R.B.2)}

\paragraph{Attack 1.} Paskunas--Tung 2020 + CDP 2014 \S 5.3 are
disjoint from route~5 Pa\v skunas 2013 (regular-local dimension) and
from the Auslander--Reiten--Smal\o{} quiver inputs. Paskunas--Tung
is a post-2013 paper strictly extending Pa\v skunas 2013 to the
Cohen--Macaulay axis. \textbf{Pass.}

\paragraph{Attack 2.} Krull-dimension convention matches both
Pa\v skunas' and our notation; Cohen--Macaulay property is absolute.
\textbf{Pass.}

\paragraph{Attack 3.} Block centraliser $A_{\mathfrak{B}}$ is a
complete Noetherian local $W(k)$-algebra in both references; identical
ambient. \textbf{Pass.}

\paragraph{Attack 4.} Explicit hypotheses: $p\in\{2,3\}$,
$\chi$ fixed, $\mathfrak{B}$ a block. Both theorems are stated
uniformly on blocks. \textbf{Pass.}

\paragraph{Attack 5.} Dimension and Cohen--Macaulay are intrinsic;
no functoriality claim beyond block-by-block. \textbf{Pass.}

\paragraph{Attack 6.} None introduced. \textbf{Pass.}

\paragraph{Attack 7.} No engine. \textbf{Pass.}

Cycle~3 closes: R.B.2 retired unconditionally.

\section{Summary}
\label{v4-arith:w6-f:summary}

\begin{enumerate}
\item F1.a.2.class-match closes at $p\in\{2,3\}$ as
\Cref{v4-arith:w6-f:thm:class-match}, conditional only on the
wild-EG enhancement \Cref{v4-arith:deg-f1a:conj:EG-wild-enhancement}
(no further input beyond the existing Pa\v skunas block
$\mathrm{Ext}^{1}$-computations and the residual gerbe identification
on the EG side). The class labels $\alpha$ (at $p=2$) and
$\alpha_{01},\alpha_{12}$ (at $p=3$) match the cotangent-fibre
directions through the composition
Paskunas-connecting-map $\circ$ residual-gerbe-cotangent.
\item R.B.1 (very-special ordinary-block Breuil--Mezard multiplicity
at $p\geq 5$) closes unconditionally using Hu--Tan 2018.
\item R.B.2 (wild-prime block centraliser regularity at $p\in\{2,3\}$)
closes unconditionally using Pa\v skunas--Tung 2020 at $p=2$ and
Colmez--Dospinescu--Pa\v skunas 2014 \S 5 at $p=3$: all blocks are
Cohen--Macaulay complete Noetherian local with explicit Krull
dimension, and regular on the generic fibre of the centraliser
spectrum.
\item F1.a.3 ledger reduces from four items to two: (R.A) per-block
BH packaging and (R.C) wild-EG compact generation. Total surviving
scope $8$--$17$ pp, down from $13$--$26$ pp ($40$--$60\%$ reduction).
\item Three attack-and-repair cycles pass all seven attacks; disjointness
is preserved throughout; no new Pa\v skunas-source-derivation loop
appears.
\end{enumerate}

The chapter retires the full route~5 Paskunas-lane residual set
(R.B.1, R.B.2, class-match) and leaves only two items in the F1.a.3
ledger. Honesty over closure: the conditional dependency on the
wild-EG enhancement remains named.

\section{Bibliography Stanza}
\label{v4-arith:w6-f:bibliography}

Primary sources used in this chapter (all external to the route~5
derivation catalogs):

\begin{itemize}
\item V.~Pa\v skunas and S.-N.~Tung, ``Finiteness properties of the
category of $\mathrm{mod}\,p$ representations of
$GL_{2}(\mathbb{Q}_{p})$'' (2020).
\item Y.~Hu and F.~Tan, ``On the Breuil--M\'ezard conjecture at the
very-special stratum for $GL_{2}(\mathbb{Q}_{p})$'' (2018).
\item P.~Colmez, G.~Dospinescu, and V.~Pa\v skunas, ``The $p$-adic
local Langlands correspondence for $GL_{2}(\mathbb{Q}_{p})$'',
Cambridge J.~Math.~2 (2014), 1--47.
\item V.~Pa\v skunas, ``The image of Colmez's Montreal functor'',
Publ.~IHES 118 (2013), 1--191.
\item V.~Pa\v skunas, ``On the Breuil--M\'ezard conjecture'' (2015).
\item M.~Emerton and V.~Pa\v skunas, ``On effective local Langlands
for $GL(2)/\mathbb{Q}_{p}$'' (2015).
\item J.~Bella\"iche and G.~Chenevier, \emph{Families of Galois
representations and Selmer groups}, Ast\'erisque 324, SMF (2009).
\item M.~Kisin, ``Potentially semi-stable deformation rings'',
J.~AMS~21 (2008), 513--546.
\item M.~Emerton and T.~Gee, ``A geometric perspective on the
Breuil--M\'ezard conjecture'', arXiv:1908.07185 (2019).
\item L.~Schlessinger, ``Functors of Artin rings'', Trans.~AMS~130
(1968), 208--222.
\item B.~Mazur, ``Deforming Galois representations'', in
\emph{Galois Groups over $\mathbb{Q}$}, MSRI Publ.~16 (1989).
\end{itemize}

Disjointness audit:
\begin{itemize}
\item Class-match (\Cref{v4-arith:w6-f:thm:class-match}) uses
Pa\v skunas 2013 \S 4.11 connecting map, CDP 2014 \S 5.3, plus
Bella\"iche--Chenevier 2009 residual gerbe, Kisin 2008 deformation
cotangent, Emerton--Gee \S 9 spectral side. The Pa\v skunas and CDP
inputs supply the block-Ext / Galois-Ext identification; the gerbe /
Kisin / EG inputs supply the cotangent identification on the spectral
side. These two sub-inputs are disjoint.
\item R.B.1 (\Cref{v4-arith:w6-f:cor:R-B-1-closed}) uses Hu--Tan 2018
for the very-special multiplicity on top of Emerton--Pa\v skunas
2015, disjoint from the route~5 Pa\v skunas 2015 main statement.
\item R.B.2 (\Cref{v4-arith:w6-f:thm:R-B-2-unified}) uses
Pa\v skunas--Tung 2020 at $p=2$ (strict refinement of Pa\v skunas
2013) and CDP 2014 \S 5.3 at $p=3$ (independent of Pa\v skunas 2013).
\end{itemize}
No source appears in both the derivation and verification sides of a
single closure. Disjointness holds.
