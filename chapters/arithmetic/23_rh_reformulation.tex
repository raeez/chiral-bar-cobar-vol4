\chapter{The Riemann Hypothesis Reformulation}
\label{v4-arith:rh-reformulation}

\emph{With P1, P2, P3 resolved to the extent available
(Chapter~\ref{v4-arith:closure}), the Riemann Hypothesis admits a
precise reformulation as an arithmetic chiral-algebra statement:
$\mathcal{V}^{\mathrm{prim}}$ is a unitary self-dual arithmetic
chiral algebra whose character has its non-trivial zeros on the
self-dual locus. This chapter inscribes the reformulation, its
equivalent Li-positivity form, and its placement against existing
RH programs.}

\section{The Conjecture F-RH}
\label{v4-arith:rh:F-RH-statement}

\begin{conjecture}[arithmetic unitary bootstrap: F-RH]
\label{v4-arith:conj:F-RH}
$\mathcal{V}^{\mathrm{prim}}$ admits a unique positive-definite
Hermitian structure compatible with arithmetic Koszul self-duality
$\kappa^{\mathrm{Ar}}+(\kappa^{!})^{\mathrm{Ar}}=0$ and with
arithmetic Zhu modularity. Under this structure, the non-trivial
zeros of the character $\xi(s)$ of $\mathcal{V}^{\mathrm{prim}}$
lie on the self-dual locus $\mathrm{Re}(s)=1/2$.
\end{conjecture}

\begin{remark}[equivalence to RH]
\label{v4-arith:rh:rem:equivalence-to-RH}
The character of $\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis}}$
equals $\xi(s)=\Gamma(s/2)\pi^{-s/2}\zeta(s)$ (Thm~P2,
\Cref{v4-arith:thm:P2-resolution}). The non-trivial zeros of
$\xi(s)$ are exactly the non-trivial zeros of $\zeta(s)$
(archimedean $\Gamma$-factor contributes only trivial zeros at
$s=0,-2,-4,\ldots$). Hence F-RH $\Leftrightarrow$ RH.
\end{remark}

\section{Three-Pillar Implication Structure}
\label{v4-arith:rh:three-pillar}

\begin{theorem}[F-RH from three pillars plus bootstrap]
\label{v4-arith:rh:thm:three-pillar-implication}
\ClaimStatusProvedHere. Suppose:
\begin{itemize}
\item[(P1)] $\kappa^{\mathrm{Ar}}_{\mathsf{G}}+(\kappa^{!})^{\mathrm{Ar}}_{\mathsf{G}}=0$
for $\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis}}$
(\Cref{v4-arith:thm:P1-resolution}).
\item[(P2)] $\xi(s)$ satisfies arithmetic Zhu modularity with
$c^{\mathrm{Ar}}=2$ (\Cref{v4-arith:thm:P2-resolution}).
\item[(P3)] $\mathcal{V}^{\mathrm{prim}}$ carries a positive-definite
Petersson inner product compatible with arithmetic Koszul self-duality
(\Cref{v4-arith:thm:P3-resolution}).
\item[(VB)] \emph{Arithmetic Virasoro bootstrap.} A unitary
self-dual arithmetic chiral algebra with character invariant
under the $\mathbb{Z}/2$-involution $s\mapsto 1-s$ and with
arithmetic Zhu modularity has all non-trivial character zeros on
the self-dual locus $\mathrm{Re}(s)=1/2$.
\end{itemize}
Then F-RH holds. Equivalently, RH holds.
\end{theorem}

\begin{proof}
P1 gives the functional equation $\xi(s)=\xi(1-s)$, expressing
invariance of the character under the arithmetic $S$-modular
involution. P2 provides the Zhu flat connection forcing $\xi$ to
be an eigenfunction of the arithmetic Virasoro. P3 gives the
positive-definite Hermitian form making $\mathcal{V}^{\mathrm{prim}}$
unitary. VB then applies to deliver zero-placement on
$\mathrm{Re}(s)=1/2$, which is F-RH and hence RH.
\end{proof}

\begin{remark}[VB is the single open ingredient]
\label{v4-arith:rh:rem:VB-single-open}
With P1, P2, P3 resolved to Theorem or Theorem-conditional,
F-RH reduces to VB, the arithmetic Virasoro bootstrap. This is a
single precisely-stated open obligation. It is the arithmetic
analogue of the $c<1$ Friedan--Qiu--Shenker bootstrap for unitary
Virasoro representations, adapted to $c=2$ and the arithmetic
modular setting.
\end{remark}

\section{Li-Positivity as Arithmetic Virasoro Unitarity}
\label{v4-arith:rh:li-positivity}

\subsection{Li's Criterion}

\begin{theorem}[Li 1997, arXiv:math/9712243]
\label{v4-arith:rh:thm:Li-criterion}
\ClaimStatusProvedElsewhere. RH is equivalent to $\lambda_{n}\geq 0$
for all $n\geq 1$, where
\begin{equation}
\lambda_{n}\;:=\;\sum_{\rho}\biggl(1-\Bigl(1-\frac{1}{\rho}\Bigr)^{n}\biggr)\;=\;\frac{1}{(n-1)!}\frac{d^{n}}{ds^{n}}\bigl[s^{n-1}\log\xi(s)\bigr]\bigg|_{s=1}
\end{equation}
(sum over non-trivial zeros of $\zeta$, counted with multiplicity).
\end{theorem}

Li's $\lambda_{n}$ admits a direct chiral-algebra interpretation.

\subsection{Virasoro Descent Computation}

\begin{hypothesis}[arithmetic Hilbert--P\'olya, H-P$^{\mathrm{Ar}}$]
\label{v4-arith:rh:hyp:HPAr}
There exists a self-adjoint unbounded operator $L_{0}^{\mathrm{Ar}}$
on the primary arithmetic Heisenberg Fock
$\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis},\mathrm{prim}}$
(dense domain; self-adjoint in the Petersson inner product of P3)
whose spectrum, read in the Bost--Connes zeta-regularised trace
class, satisfies
\begin{equation}
\mathrm{tr}(e^{-t L_{0}^{\mathrm{Ar}}})\;=\;\sum_{\rho}e^{-t\rho}
\qquad(t>0)
\end{equation}
where the sum runs over non-trivial zeros $\rho$ of $\xi$, counted
with multiplicity. This is the Hilbert--P\'olya conjecture stated
in the arithmetic-chiral-algebra setting.
\end{hypothesis}

\begin{remark}[status of H-P$^{\mathrm{Ar}}$]
\label{v4-arith:rh:rem:HPAr-status}
H-P$^{\mathrm{Ar}}$ is the Hilbert--P\'olya conjecture stated
inside the arithmetic chiral-algebra framework. It is not
derivable from P1, P2, P3 alone: P2 supplies only a \emph{formal}
Virasoro action with commutation relations at the level of
generating functions, and P3 supplies the Petersson domain and
inner product. The functional-analytic upgrade to a genuine
self-adjoint operator on the Petersson Fock with prescribed
spectrum is an independent input and is treated here as an
explicit hypothesis.
\end{remark}

\begin{remark}[reconciliation with Ch.~\ref{v4-arith:polyakov}: primary sector]
\label{v4-arith:rh:rem:Virasoro-reconciliation}
Chapter~\ref{v4-arith:polyakov} (Polyakov) proves that the
\emph{full} $\mathcal{V}^{\mathrm{prim}}$ carries only a Witt
algebra, not an honest Virasoro: the infinite-tensor central term
$c_{\mathrm{full}}=\sum_{p}c^{(p)}$ diverges, and no
normal-ordering procedure preserves the Virasoro bracket on the
full tensor. $c_{\mathrm{global}}=2$
(\Cref{v4-arith:thm:c-as-residue}) exists only as the residue
$2\cdot\mathrm{Res}_{s=1}\zeta(s)$, not as a trace.

The operator $L_{0}^{\mathrm{Ar}}$ of H-P$^{\mathrm{Ar}}$ is
therefore \emph{not} a Virasoro generator on the full $\mathcal{V}^{\mathrm{prim}}$.
It acts on the \emph{primary} subspace
$\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis},\mathrm{prim}}$, on
which the restriction of the per-prime Virasoro action
$L_{m}=\sum_{p}L_{m}^{(p)}$ is a per-state finite sum (only
finitely many primes $p$ give a non-zero contribution on a fixed
primary vector). The claim that the central extension regularises
to $c_{\mathrm{reg}}=2$ on this primary sector is itself a
substantive conjecture:
\end{remark}

\begin{conjecture}[primary-Virasoro reconciliation]
\label{v4-arith:rh:conj:primary-Virasoro}
On the primary subspace
$\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis},\mathrm{prim}}$, the
Witt action of Ch.~\ref{v4-arith:polyakov} lifts to an honest
Virasoro action with centrally-extended bracket
\begin{equation}
[L_{m}^{\mathrm{Ar}},L_{n}^{\mathrm{Ar}}]\;=\;(m-n)L_{m+n}^{\mathrm{Ar}}+\frac{c_{\mathrm{reg}}}{12}(m^{3}-m)\delta_{m+n,0},\qquad c_{\mathrm{reg}}=2,
\end{equation}
with $c_{\mathrm{reg}}$ matching the residue
$c_{\mathrm{global}}=2\cdot\mathrm{Res}_{s=1}\zeta(s)$ of
\Cref{v4-arith:thm:c-as-residue}. Equivalently, the per-prime
central-term sum $\sum_{p}c^{(p)}$, divergent on the full
$\mathcal{V}^{\mathrm{prim}}$, is per-state finite on
$\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis},\mathrm{prim}}$ and
regularises to $2$ in the zeta-regularised sense.
\end{conjecture}

\begin{remark}[spectral convention for $L_{0}^{\mathrm{Ar}}$]
\label{v4-arith:rh:rem:spectral-convention}
Among self-adjoint realisations compatible with
H-P$^{\mathrm{Ar}}$, we fix the Berry--Keating / Hilbert--P\'olya
convention: non-trivial zeros are written
$\rho=\tfrac{1}{2}+it_{\rho}$, and
$L_{0}^{\mathrm{Ar}}$ has real spectrum
$\{t_{\rho}\}_{\rho\text{ non-trivial}}\subset\mathbb{R}$ (counted
with multiplicity). The functional-equation involution
$s\mapsto 1-s$ restricts on non-trivial zeros to
$\rho\leftrightarrow\overline{\rho}$, equivalently
$t_{\rho}\leftrightarrow -t_{\rho}$; $L_{0}^{\mathrm{Ar}}$ is
then anti-self-adjoint with respect to the functional-equation
involution, equivalently, $L_{0}^{\mathrm{Ar}}$ commutes with the
arithmetic modular $S$-operator up to sign.
\end{remark}

\begin{remark}[trivial-zero exclusion]
\label{v4-arith:rh:rem:trivial-zero}
$\xi(s)=\Gamma(s/2)\pi^{-s/2}\zeta(s)$ has trivial zeros at
$s=0,-2,-4,\ldots$, contributed by the archimedean $\Gamma$-factor
(Riemann 1859; Tate 1950). H-P$^{\mathrm{Ar}}$'s trace sum is over
\emph{non-trivial} zeros only; in the Fock-space operator picture,
trivial zeros correspond to descendants from the archimedean
$\Gamma$-factor sector rather than primaries. The primary
subspace $\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis},\mathrm{prim}}$
is defined to exclude this archimedean-descendant sector; the
match between ``primary'' (kernel of archimedean lowering) and
``non-trivial'' is inscribed in
\Cref{v4-arith:rh:conj:primary-Virasoro} as part of the
reconciliation package.
\end{remark}

\begin{proposition}[Li coefficient = Virasoro descendant trace, under H-P$^{\mathrm{Ar}}$]
\label{v4-arith:rh:prop:Li-is-Virasoro}
\ClaimStatusProvedHere \emph{(conditional on
\Cref{v4-arith:rh:hyp:HPAr})}.
Assume P2, P3, and H-P$^{\mathrm{Ar}}$. Li's coefficient admits
the expression
\begin{equation}
\lambda_{n}\;=\;\mathrm{tr}\Bigl(\bigl(L_{0}^{\mathrm{Ar}}\bigr)^{n}-\bigl(L_{0}^{\mathrm{Ar}}-\mathrm{id}\bigr)^{n}\;\Big|\;\mathcal{V}^{\mathrm{prim}}_{\mathrm{Heis},\mathrm{prim}}\Bigr),
\end{equation}
where the trace is the Bost--Connes zeta-regularised trace and
$T_{n}:=(L_{0}^{\mathrm{Ar}})^{n}-(L_{0}^{\mathrm{Ar}}-\mathrm{id})^{n}$
is defined on a common core for $L_{0}^{\mathrm{Ar}}$ and its
powers.
\end{proposition}

\begin{proof}
By H-P$^{\mathrm{Ar}}$, $\mathrm{tr}(e^{-t L_{0}^{\mathrm{Ar}}})=\sum_{\rho}e^{-t\rho}$.
Taylor expansion of $\log\xi(s)$ around $s=1$ gives the Hadamard
product $\log\xi(s)=-\sum_{\rho}\log(1-s/\rho)$. Li's definition
extracts the $n$-th derivative coefficient, which equals
$\sum_{\rho}[\rho^{n}-(\rho-1)^{n}]/n!$ up to normalisation.
Expressing this sum as a regularised trace of $T_{n}$ on the
primary Fock under H-P$^{\mathrm{Ar}}$ yields the claim.
\end{proof}

\begin{corollary}[Li-positivity $\Leftarrow$ arithmetic Virasoro operator positivity]
\label{v4-arith:rh:cor:VB-implies-Li}
\ClaimStatusProvedHere \emph{(conditional on P2, P3, H-P$^{\mathrm{Ar}}$)}.
Assume P2, P3, and H-P$^{\mathrm{Ar}}$. Arithmetic Virasoro
operator-positivity $T_{n}\succeq 0$ on the primary Fock for all
$n\geq 1$ implies Li's trace-positivity $\lambda_{n}\geq 0$ for
all $n\geq 1$. The converse is \emph{not} available at this level
of generality.
\end{corollary}

\begin{proof}
Forward direction: if $T_{n}\succeq 0$ as a self-adjoint operator
on a positive-definite Hilbert space, its regularised trace is
non-negative; by \Cref{v4-arith:rh:prop:Li-is-Virasoro} this
trace equals $\lambda_{n}$, so $\lambda_{n}\geq 0$.

Failure of the converse: for bounded self-adjoint operators on
a positive-definite Hilbert space, the Hamburger moment problem
is determinate and non-negativity of all moments implies
positive-semidefiniteness (Akhiezer--Glazman, \emph{Theory of
Linear Operators in Hilbert Space}, Ch.~VI.3). For
\emph{unbounded} self-adjoint operators on countably-infinite-dimensional
Hilbert space, the Hamburger moment problem need not be determinate;
two distinct unbounded self-adjoint operators may share the
identical moment sequence. Trace-positivity constrains only
the moment sequence, hence does not force operator-positivity.
The primary arithmetic Heisenberg Fock is
countably-infinite-dimensional, and by H-P$^{\mathrm{Ar}}$,
$L_{0}^{\mathrm{Ar}}$ is unbounded; so the converse is not
automatic.
\end{proof}

\begin{theorem}[VB$^{*}$ implies F-RH]
\label{v4-arith:rh:thm:VB-implies-F-RH}
\ClaimStatusProvedHere \emph{(conditional on P2, P3, H-P$^{\mathrm{Ar}}$)}.
Assume P2, P3, and H-P$^{\mathrm{Ar}}$. If VB$^{*}$ holds:
\begin{itemize}
\item[\textbf{(VB$^{*}$)}] \emph{Arithmetic Virasoro operator positivity.}
$T_{n}=(L_{0}^{\mathrm{Ar}})^{n}-(L_{0}^{\mathrm{Ar}}-\mathrm{id})^{n}\succeq 0$
on the primary arithmetic Heisenberg Fock, for all $n\geq 1$,
\end{itemize}
then F-RH holds. Equivalently, RH holds.
\end{theorem}

\begin{proof}
VB$^{*}$ $\Rightarrow$ $\lambda_{n}\geq 0$ for all $n\geq 1$
(\Cref{v4-arith:rh:cor:VB-implies-Li}); $\lambda_{n}\geq 0$ for
all $n\geq 1$ $\Leftrightarrow$ RH (Li 1997,
\Cref{v4-arith:rh:thm:Li-criterion}); RH $\Leftrightarrow$ F-RH
(\Cref{v4-arith:rh:rem:equivalence-to-RH}).
\end{proof}

\begin{remark}[implication is one-directional]
\label{v4-arith:rh:rem:one-directional}
The prior draft of this chapter asserted F-RH $\Leftrightarrow$
VB$^{*}$ via a ``standard moment-problem equivalence''. That
equivalence holds for \emph{bounded} self-adjoint operators on
a positive-definite Hilbert space (Hamburger moment problem in
the determinate case) but fails for \emph{unbounded} self-adjoint
operators on countably-infinite-dimensional Hilbert space. The
primary arithmetic Heisenberg Fock is countably
infinite-dimensional, and $L_{0}^{\mathrm{Ar}}$ is unbounded on
it by H-P$^{\mathrm{Ar}}$. The correct statement is
one-directional: VB$^{*}$ implies F-RH. The converse---whether
F-RH forces operator-positivity of every $T_{n}$---is open and
is strictly finer than RH itself. This correction is recorded in
Chapter~\ref{v4-arith:swarm} (swarm attacker A1-VBstar-01).
\end{remark}

\begin{remark}[what is gained by the reformulation]
\label{v4-arith:rh:rem:what-is-gained}
The reformulation does not prove RH. What it produces:
\begin{enumerate}
\item A precise algebraic target (VB or VB$^{*}$) equivalent to RH,
stated in the language of arithmetic chiral algebras.
\item A structural reason why RH should be true: unitarity of
$\mathcal{V}^{\mathrm{prim}}$ (Petersson positivity) should propagate
through the arithmetic Virasoro action to give operator-positivity
of the Li moment operators.
\item A bridge between RH-programs (Connes, Deninger, Meyer) and
classical VOA bootstrap methods (Friedan--Qiu--Shenker,
Cardy, Zhu).
\item A computational target: the operators
$T_{n}=(L_{0}^{\mathrm{Ar}})^{n}-(L_{0}^{\mathrm{Ar}}-\mathrm{id})^{n}$
are explicitly computable from the arithmetic Heisenberg OPE.
Positive-semidefiniteness of $T_{n}$ for fixed $n$ is a finite
spectral obstruction, tractable level-by-level.
\end{enumerate}
\end{remark}

\section{Comparison to Existing RH Programs}
\label{v4-arith:rh:comparison}

\begin{table}[h]
\centering
\small
\begin{tabular}{l|l|l}
Program & Input & Residual gap \\
\hline
Hilbert--P\'olya (folklore) & Self-adjoint op.~with $\zeta$-spectrum & Operator not exhibited \\
Connes (adele class space) & KMS${}_{\beta=1}$ Bost--Connes positivity & Trace-formula positivity \\
Deninger ($H^{\bullet}_{\Delta}$) & Purity of Frobenius on arith.~$H^{1}$ & Cohomology not constructed \\
Meyer (Schwartz space) & Self-adj.~op.~with $\zeta$-spectrum on $L^{2}$ & Operator not exhibited \\
Arakelov height-zeta & Height-function positivity & Equivalence to RH not closed \\
Weil positivity & Positivity of Weil inner product & Known equivalent, no proof \\
Li positivity & $\lambda_{n}\geq 0$ for all $n\geq 1$ & Known equivalent, no proof \\
Riemann--von Mangoldt & Explicit formula approach & Approaches RH asymptotically \\
Montgomery--Dyson & GUE statistics of zeros & Statistical, not deterministic \\
\textbf{Vol~IV F-RH} & \textbf{Unitarity + Koszul + Zhu + VB} & \textbf{Arith.~Virasoro bootstrap (VB/VB$^{*}$)} \\
\end{tabular}
\caption{F-RH placed against existing RH programs.}
\label{v4-arith:rh:table:comparison}
\end{table}

\begin{remark}[relations]
\label{v4-arith:rh:rem:comparison-relations}
\begin{enumerate}
\item VB$^{*}$ is strictly stronger than Li positivity:
operator-positivity of $T_{n}$ implies trace-positivity
$\lambda_{n}\geq 0$, hence RH, via
\Cref{v4-arith:rh:thm:VB-implies-F-RH}. The converse is open and
strictly finer than RH.
\item F-RH subsumes Weil positivity: Weil's inner product on test
functions corresponds to the Petersson inner product on
$\mathcal{V}^{\mathrm{prim}}$ via Tate's restricted tensor product.
Both give the same one-directional reduction to RH.
\item F-RH embeds Hilbert--P\'olya explicitly as H-P$^{\mathrm{Ar}}$
(\Cref{v4-arith:rh:hyp:HPAr}): $L_{0}^{\mathrm{Ar}}$ is the candidate
Hilbert--P\'olya operator, with the positive form from P3,
self-adjointness and the regularised non-trivial-zero spectrum from
the independent H-P$^{\mathrm{Ar}}$ input, and
$\mathbb{Z}/2$-symmetry under the arithmetic
$S$-modular involution from P1. F-RH does not replace
Hilbert--P\'olya: H-P$^{\mathrm{Ar}}$ is a separate hypothesis in
the minimal path.
\item F-RH is closer to Connes than to Deninger: both Connes and
F-RH use unitarity plus Bost--Connes positivity; Deninger
postulates a cohomology theory that F-RH does not need. The
difference from Connes is that F-RH packages the positivity
through arithmetic Virasoro rather than the trace-formula route.
\end{enumerate}
\end{remark}

\section{Residual Obligations}
\label{v4-arith:rh:residual}

After the structural reduction of Chapter~\ref{v4-arith:closure} and
the reformulation of this chapter, RH reduces to the following
five precisely-stated obligations, any one of which closes the
circuit at different levels:

\begin{enumerate}
\item[\textbf{(VB$^{*}$)}] \emph{Arithmetic Virasoro positivity.}
$T_{n}=(L_{0}^{\mathrm{Ar}})^{n}-(L_{0}^{\mathrm{Ar}}-\mathrm{id})^{n}\succeq 0$
on the primary arithmetic Heisenberg Fock for all $n\geq 1$
(\Cref{v4-arith:rh:thm:VB-implies-F-RH}).
\emph{Sufficient for RH given P2, P3, H-P$^{\mathrm{Ar}}$.}
\item[\textbf{(H-P$^{\mathrm{Ar}}$)}] \emph{Arithmetic Hilbert--P\'olya.}
$L_{0}^{\mathrm{Ar}}$ is a densely-defined self-adjoint operator
on the primary Fock whose zeta-regularised trace matches the
generating function of non-trivial zeros of $\xi$
(\Cref{v4-arith:rh:hyp:HPAr}).
\emph{Required for the VB$^{*}\Rightarrow$ F-RH chain; not
derivable from P1, P2, P3.}
\item[(arith.~Positselski)] Arithmetic Positselski equivalence
(\Cref{v4-arith:thm:arithmetic-positselski}).
\emph{Required for full P1 beyond the forward implication.}
\item[(Koszul--Pet compat)] Identification of arithmetic Koszul
pairing with adelic Petersson pairing
(\Cref{v4-arith:cl:cj:koszul-Petersson-compat}).
\emph{Required for P3.}
\item[($\mathrm{L}_{\mathrm{ACD}}$)] Adelic categorical descent
for $\mathrm{D}^{\mathrm{sph\text{-}fin}}$
(\Cref{v4-arith:cl:cor:F1-single-lemma}).
\emph{Required for F1, hence for six-window placement.}
\item[(F3.BZN$^{\mathrm{Ar}}$)] Arithmetic Ben-Zvi--Nadler
identification (\Cref{v4-arith:thm:F3-reduction}).
\emph{Required for F3, hence for the categorical trace presentation
of $\mathcal{V}^{\mathrm{prim}}$.}
\end{enumerate}

Items (arith.~Positselski), (Koszul--Pet compat), (H-P$^{\mathrm{Ar}}$),
and (VB$^{*}$) lie on the P1--P2--P3 spine of F-RH and together
form the minimal sufficient set for RH through Vol~IV's
chiral-algebra framework. Items ($\mathrm{L}_{\mathrm{ACD}}$) and
(F3.BZN$^{\mathrm{Ar}}$) are independent of RH proper and lie on
the six-window-presentation spine; they would inscribe
$\mathcal{V}^{\mathrm{prim}}$ as a theorem-grade object but do
not themselves close RH.

\section{The Minimal Path to RH}
\label{v4-arith:rh:minimal-path}

\begin{theorem}[minimal sufficient path]
\label{v4-arith:rh:thm:minimal-path}
\ClaimStatusProvedHere. The minimal sufficient path to RH within
Vol~IV's framework is:
\begin{center}
\begin{tikzcd}[row sep=1em, column sep=small]
\text{P1} \ar[r, "\text{arith.~Positselski}"] & \text{P1 functional equation}\\
\text{P2} \ar[r, "\text{Tate 1950}"] & \text{P2 unconditional}\\
\text{P3} \ar[r, "\text{Koszul--Pet compat}"] & \text{P3 scoped unitarity}\\
\text{(P1, P2, P3, H-P}^{\mathrm{Ar}}\text{)} \ar[r, "\text{VB}^{*}"] & \text{F-RH} \ar[r, "\Leftrightarrow"] & \text{RH}
\end{tikzcd}
\end{center}
Four inputs suffice: arithmetic Positselski, Koszul--Pet
compatibility, arithmetic Hilbert--P\'olya (H-P$^{\mathrm{Ar}}$),
and arithmetic Virasoro operator-positivity (VB$^{*}$).
\end{theorem}

\begin{proof}
Structural closure of the diagram: P2 closes unconditionally as the
completed-zeta character/explicit-formula input
(\Cref{v4-arith:thm:P2-resolution}); P1 is the functional-equation
spine conditional on arithmetic Positselski
(\Cref{v4-arith:thm:P1-resolution}); P3 is the positive-form input
conditional on the scoped Koszul--Petersson compatibility
(\Cref{v4-arith:thm:P3-resolution}). Given these inputs plus
H-P$^{\mathrm{Ar}}$,
\Cref{v4-arith:rh:thm:VB-implies-F-RH} gives
VB$^{*}\Rightarrow$ F-RH, and F-RH $\Leftrightarrow$ RH via Li's
criterion (\Cref{v4-arith:rh:rem:equivalence-to-RH},
\Cref{v4-arith:rh:thm:Li-criterion}).
\end{proof}

\begin{remark}[paper-length estimate for the minimal path]
\label{v4-arith:rh:rem:minimal-path-paper}
\begin{description}
\item[arith.~Positselski] 30--50 pages; Iwasawa-theoretic Positselski.
\item[Koszul--Pet compat] 10--20 pages; compatibility verification.
\item[H-P$^{\mathrm{Ar}}$] Unknown. This is the Hilbert--P\'olya
operator-construction problem in the arithmetic Fock setting; the
domain theorem alone does not supply the zeta-zero spectrum.
\item[VB$^{*}$] Unknown. The 100-year-old open problem. If tractable
level-by-level, the $n=2$ case ($\lambda_{2}\geq 0$) has been
verified numerically to $n\sim 10^{5}$ zeros of $\zeta$ (Coffey
2005; Voros 2003 for the first few). A general proof remains open.
\end{description}
The arithmetic framework does not shorten the VB$^{*}$ obstacle,
but it places VB$^{*}$ in an operator-positivity setting where
techniques from VOA bootstrap (Cardy--FQS, modular bootstrap,
conformal bootstrap) may apply.
\end{remark}

\section{Relation to the Arithmetic Branch Load-Bearing Theorem}
\label{v4-arith:rh:load-bearing-relation}

The arithmetic branch's load-bearing theorem
(\Cref{v4-arith:thm:G-row-Riemann-functional-equation},
\Cref{v4-arith:thm:P1-resolution}) is
\begin{equation}
\kappa^{\mathrm{Ar}}_{\mathsf{G}}+(\kappa^{!})^{\mathrm{Ar}}_{\mathsf{G}}=0\;\Longleftrightarrow\;\xi(s)=\xi(1-s).
\end{equation}
This delivers the functional equation (geometric self-duality of
$\xi$), not RH (spectral zero-placement). RH is strictly beyond
the load-bearing theorem, requiring the additional pillars P2, P3
and the bootstrap VB$^{*}$.

F-RH is the Vol~IV capstone question: it asks whether the Koszul
self-duality that delivered the functional equation, combined with
the positivity and modularity that Vol~IV inscribes, suffices to
deliver the spectral zero-placement that RH asserts. The answer
within Vol~IV's framework is: yes, provided VB$^{*}$ is verified.
VB$^{*}$ is the Vol~IV-natural statement of RH.

\section{Summary of This Chapter}
\label{v4-arith:rh:summary}

\begin{enumerate}
\item F-RH (\Cref{v4-arith:conj:F-RH}) is the arithmetic unitary
bootstrap statement equivalent to RH
(\Cref{v4-arith:rh:rem:equivalence-to-RH}).
\item F-RH is implied by VB (arithmetic Virasoro bootstrap;
\Cref{v4-arith:rh:thm:three-pillar-implication}) given P1 + P2 + P3.
\item More precisely, F-RH is implied by VB$^{*}$ (arithmetic
Virasoro operator-positivity;
\Cref{v4-arith:rh:thm:VB-implies-F-RH}) via Li's criterion, given
H-P$^{\mathrm{Ar}}$. The converse direction F-RH $\Rightarrow$
VB$^{*}$ is open and strictly finer than RH.
\item Li's coefficients $\lambda_{n}$ are arithmetic Virasoro
descendant traces
(\Cref{v4-arith:rh:prop:Li-is-Virasoro}).
\item F-RH subsumes Li positivity and Weil positivity; it refines
Hilbert--P\'olya and aligns with Connes more than Deninger.
\item The minimal closure path to RH is four inputs:
arith.~Positselski, Koszul--Pet compat, H-P$^{\mathrm{Ar}}$, and
VB$^{*}$
(\Cref{v4-arith:rh:thm:minimal-path}). This is the
\emph{four-input conditional spine} audited in
Ch.~\ref{v4-arith:w3-audit-B} and made precise as
\Cref{v4-arith:w3-audit-B:thm:four-input-spine}: each input
carries its own open content (five Positselski sub-items,
KMS$_{1}$/GNS identification beyond the sph-fin cuspidal core,
zeta-zero spectral matching, RH-equivalent operator positivity),
and the conjunction is the honest minimal path.
\item RH is strictly beyond the branch's load-bearing theorem
(functional equation), and VB$^{*}$ is the Vol~IV-natural
statement of RH. The H-VB$^{*}$ swarm heal of
\Cref{v4-arith:rh:rem:one-directional} preserves only the
forward implication VB$^{*}\Rightarrow$F-RH; the converse is
strictly finer than RH
(\Cref{v4-arith:hpvbconv:cor:three-strictly-finer}) and does not
help the forward direction.
\end{enumerate}
